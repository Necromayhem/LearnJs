// Задачи от gpt

// Уровень 1: Начальный
// 1) Отложенное сообщение:
// Напишите функцию, которая использует setTimeout, чтобы вывести в консоль сообщение "Привет, мир!" через 3 секунды после запуска программы.

function printHello(){
    console.log("Привет, мир!");
}

// setTimeout(printHello, 3000)



// 2) Повторяющиеся числа:
// Используя setInterval, напишите скрипт, который выводит в консоль числа от 1 до 5, с интервалом в 1 секунду между выводами. После достижения 5, остановите интервал.

let timerId = setInterval(() => console.log(n++), 1000);

setTimeout(() => clearInterval(timerId), 5000, n = 1)



// Уровень 2: Средний
// 1) Таймер с обратным отсчетом:
// Напишите функцию, которая принимает число секунд и использует setInterval для обратного отсчета до нуля, выводя каждую секунду оставшееся время. После достижения нуля остановите интервал и выведите сообщение "Время вышло!".

function tick(n) {
    if (n > 0) {
        console.log(n);
        n--;
        setTimeout(tick, 1000, n);
    } else if (n === 0) {
        console.log("Время вышло!");
    }
}

setTimeout(tick, 1000, 10);


// 2) Асинхронная цепочка:
// Создайте цепочку из трех setTimeout, так чтобы каждое сообщение "Шаг 1", "Шаг 2" и "Шаг 3" выводилось в консоль с интервалом в 2 секунды.

// setTimeout(step1, 2000);

function step1(){
    console.log("Шаг 1");
    setTimeout(step2, 2000)
}

function step2(){
    console.log("Шаг 2");
    setTimeout(step3, 2000)
}

function step3(){
    console.log("Шаг 3");
}



// Уровень 3: Продвинутый
// 1) Вдох и выдох:
// Симулируйте упражнение на дыхание, используя setInterval. Каждые 4 секунды должна происходить смена состояния: "Вдох", "Задержка", "Выдох", и снова "Задержка". Каждое состояние длится 4 секунды. После трех циклов остановите упражнение.

let breath = setInterval(f1, 0)

let round = 0;

function f1(){
    round++
    setTimeout(console.log('Вдох'), 4000)
    
}





